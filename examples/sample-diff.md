# Drift report (sample)

Generated by `comfyguard diff --against baseline.json /opt/comfyui`. Ruleset
2026.08.1. Baseline captured 2026-08-03; compared against the live instance on
2026-08-10.

**Grade: F.** Set by a high-confidence tamper: a custom-node file changed without
its git commit changing (DRIFT-001), and a known-malicious node appeared
(DRIFT-002). This is what `diff` is for. An instance that graded clean a week ago
now shows the classic shape of a post-compromise: modified code, a planted node,
security turned down, and the door opened to the network.

Nothing here changed the instance. This is a read-only comparison. To roll back to
the clean baseline, use `comfyguard restore baseline.json`.

## What changed

| Check | Severity | What | Class |
|---|---|---|---|
| DRIFT-001 | Critical | `custom_nodes/handy-tools/util.py` content changed, git commit unchanged | tamper |
| DRIFT-002 | Critical | New node `ComfyUI-Upscaler-4K` appeared, matches a known info-stealer (PATCH-007) | tamper |
| DRIFT-008 | High | Manager `security_level` strong to weak; `allow_pip_install` false to true | config downgrade |
| DRIFT-009 | High | `--listen` widened from `127.0.0.1` to `0.0.0.0` | exposure regression |
| DRIFT-011 | Medium | Core commit changed (still 0.3.62, not a known-vulnerable range) | stability |

## Read this first

The two Critical findings mean the host should be treated as possibly compromised.
DRIFT-001 in particular is hard to explain benignly: source that matches a pinned
commit should hash identically, so a hash change with an unchanged commit means
the file was edited out-of-band.

## Suggested response

1. Isolate the instance from the network now (DRIFT-009).
2. Quarantine `ComfyUI-Upscaler-4K` and `handy-tools`, and investigate persistence
   on the host (DRIFT-001, DRIFT-002).
3. Restore `security_level` and the bind address (DRIFT-008, DRIFT-009).
4. Once contained, roll back to the clean baseline with
   `comfyguard restore baseline.json`, then run `comfyguard diff --against
   baseline.json /opt/comfyui` and expect an empty diff.

The confirm-and-remediate work is for a coding agent or operator, under the gates
in each finding. `comfyguard` only reported it.
